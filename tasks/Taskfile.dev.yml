version: '3'

tasks:
  # ============================================================================
  # DEVELOPMENT TASKS (Only keeping the ones that are actually used)
  # ============================================================================

  dev-only:
    desc: Start Next.js development server only (without Supabase)
    cmds:
      - |
        echo "🚀 Starting Next.js development server..."
        npm run dev 